# tests/

Test suite:

- **integration/** — API endpoint tests using Supertest against the Express app.
- **unit/** — Isolated unit tests for services and utilities.
- **helpers/** — Test setup, factories, and shared test utilities.
- **vitest.config.ts** — Test runner configuration.
