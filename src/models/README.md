# models/

Database schema definitions:

- **prisma/** — Reference copy of the Prisma schema (canonical version lives at `prisma/schema.prisma`).
- **drizzle/** — Drizzle ORM table definitions and migrations (alternative to Prisma).
