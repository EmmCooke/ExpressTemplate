# validators/

Zod schema definitions for request validation. Each resource has its own validator file. Schemas are used by the `validate` middleware to parse and type request bodies, query strings, and params.
