# types/

Shared TypeScript type definitions:

- **express.d.ts** — Express Request type augmentations (e.g., `req.user`).
- **environment.d.ts** — `process.env` type declarations.
- **common.ts** — Shared interfaces used across layers.
